---
tags: [ai-security, шаблон, mcp-review]
статус: шаблон
---

# Шаблон: MCP Server Security Review

[← Оглавление](../README.md)

> Заполняемая форма для ревью MCP-сервера перед подключением. Контекст — в [19 — MCP Security](../notes/part-6-multi-agent-security/19-mcp-security.md) (включая MCP03 Tool Poisoning).

## 1. Идентификация сервера

- **Server ID / name:** `<...>`
- **Origin / источник:** `<репозиторий, vendor, локальный>`
- **Версия (pinned):** `<vX.Y / commit hash>`
- **Owner:** `<кто отвечает за подключение>`
- **Транспорт:** `<stdio / http / sse>`

## 2. Capabilities

| Возможность | Заявлено | Разрешаем |
|---|---|---|
| Tools | `<список>` | `<allowlist>` |
| Resources | `<...>` | `<...>` |
| Prompts | `<...>` | `<...>` |
| Sampling | `<да/нет>` | `<...>` |

## 3. Проверка tool poisoning (MCP03)

- [ ] Описания tools не содержат скрытых инструкций для модели.
- [ ] Описание соответствует фактическому поведению (нет capability lie).
- [ ] Зафиксирована версия — защита от rug pull (подмены после одобрения).
- [ ] Tool output трактуется как данные, не как команды.
- [ ] Нет cross-server влияния / shadow-сервера с тем же именем.

## 4. Контроли

- **Schema validation входов/выходов:** `<да/нет>`
- **Egress сервера:** `<куда ходит, allowlist>`
- **Sandbox / изоляция процесса:** `<...>`
- **Секреты, передаваемые серверу:** `<минимизация>`
- **Kill-switch / отключение:** `<как>`
- **Логирование вызовов:** `<...>`

## 5. Риск и мониторинг

- **Risk level:** `<Low / Medium / High>`
- **Что мониторим:** `<аномальные вызовы, новые tools, изменение описаний>`
- **Алерты:** `<...>`

## 6. Решение

- **Вердикт:** `<approve / approve с ограничениями / reject>`
- **Ограничения / allowlist:** `<...>`
- **Ревьюер / дата:** `<..., YYYY-MM-DD>`
- **Дата пересмотра:** `<...>`
