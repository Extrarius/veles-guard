---
tags: [ai-security, шаблон, tool-review]
статус: шаблон
---

# Шаблон: Tool Security Review

[← Оглавление](../README.md)

> Заполняемая форма для ревью инструмента перед выдачей агенту. Принципы — в [06 — RBAC и Tool Permissions](../notes/part-3-processing-security/06-rbac-tool-permissions.md) и [07 — Parameter Validation и Schema Enforcement](../notes/part-3-processing-security/07-parameter-validation-schema.md).

## 1. Идентификация

- **Tool:** `<имя>`
- **Назначение:** `<что делает, зачем агенту>`
- **Версия / источник:** `<...>`
- **Owner:** `<...>`

## 2. Права и доступ

- **Scopes / permissions:** `<минимально необходимые>`
- **Роли, которым разрешён вызов:** `<RBAC>`
- **Доступ к системам:** `<DB / shell / network / FS>`

## 3. Входные параметры

| Параметр | Тип | Обязательный | Валидация / schema | Опасные значения |
|---|---|---|---|---|
| `<name>` | `<string/int/...>` | да/нет | `<regex / enum / range>` | `<...>` |

- **Schema enforcement:** `<да/нет, где>`
- **Десериализация недоверенного ввода:** `<...>`

## 4. Side effects и риск

- **Идемпотентность:** `<да/нет>`
- **Изменяет внешнее состояние:** `<да/нет, что именно>`
- **Необратимые действия:** `<delete / send / payment / deploy>`
- **Egress (исходящий трафик):** `<куда, allowlist>`
- **Risk level:** `<Low / Medium / High>`

## 5. Контроли

- [ ] Sandbox / изоляция выполнения: `<...>`
- [ ] Human approval для опасных действий: `<условие>`
- [ ] Rate limit / quota: `<...>`
- [ ] Лимиты транзакций (суммы, объёмы): `<...>`
- [ ] Логирование вызова с reason / correlation ID: `<...>`
- [ ] Egress-контроль на allowlist: `<...>`

## 6. Тесты

- [ ] Невалидные параметры отклоняются.
- [ ] Вызов вне роли запрещён (deny by default).
- [ ] Injection в параметрах не приводит к выполнению.
- [ ] Опасное действие требует approval.

## 7. Решение

- **Вердикт:** `<approve / approve с ограничениями / reject>`
- **Ограничения:** `<...>`
- **Ревьюер / дата:** `<..., YYYY-MM-DD>`
